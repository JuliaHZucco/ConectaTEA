<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testando">

        <!-- TELAS DO APP -->
        <activity android:name=".TeacherHomeActivity" />
        <activity android:name=".ParentHomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AuthorizedTablesActivity" />
        <activity android:name=".RequestAccessActivity" />
        <activity android:name=".ManageTableAccessActivity" />
        <activity android:name=".TableDetailsActivity" />
        <activity android:name=".AddTableActivity" />
        <activity android:name=".ChildTablesActivity" />
        <activity android:name=".ChildrenListActivity" />
        <activity android:name=".AddChildActivity" />
        <activity android:name=".AddPictogramActivity" />


        <!-- TELA PRINCIPAL -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Testando">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>